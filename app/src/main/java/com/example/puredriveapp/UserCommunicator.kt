package com.example.puredriveapp

import androidx.appcompat.app.AppCompatActivity

interface UserCommunicator {

    //fun loadNextActivity(appCompatActivity: AppCompatActivity, )
    //fun getData(userType: String)
}